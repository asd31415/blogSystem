<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.myblog.mapper.NoticeMapper" >

    <resultMap id="BaseResultMap" type="com.myblog.entity.Notice" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="title" property="title" jdbcType="VARCHAR" />
        <result column="content" property="content" jdbcType="LONGVARCHAR" />
        <result column="createTime" property="createTime" jdbcType="TIMESTAMP" />
        <result column="updateTime" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="status" property="status" jdbcType="INTEGER" />
        <result column="order" property="order" jdbcType="INTEGER" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, title, content, createTime, updateTime, status, `order`
    </sql>

    <sql id="tb">t_notice</sql>

    <!-- 根据ID获取通知 -->
    <select id="getNoticeById" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
        select
        <include refid="Base_Column_List" />
        from <include refid="tb"/>
        where id = #{id,jdbcType=INTEGER}
    </select>

    <!-- 删除通知 -->
    <delete id="deleteById" parameterType="java.lang.Integer" >
        delete from <include refid="tb"/>
        where id = #{id,jdbcType=INTEGER}
    </delete>

    <!-- 插入通知 -->
    <insert id="insert" parameterType="com.myblog.entity.Notice" useGeneratedKeys="true" keyProperty="id">
        insert into <include refid="tb"/>
        (title, content, createTime, updateTime, status, `order`)
        values (#{title,jdbcType=VARCHAR}, #{LONGVARCHAR},
        #{createTime,jdbcType=TIMESTAMP}, #{updateTime,jdbcType=TIMESTAMP},
        #{status,jdbcType=INTEGER}, #{order,jdbcType=INTEGER})
    </insert>

    <!-- 更新通知 -->
    <update id="update" parameterType="com.myblog.entity.Notice" >
        update <include refid="tb"/>
        <set >
            <if test="title != null" >
                title = #{title,jdbcType=VARCHAR},
            </if>
            <if test="content != null" >
                content = #{content,jdbcType=TEXT},
            </if>
            <if test="createTime != null" >
                createTime = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updateTime != null" >
                updateTime = #{updateTime,jdbcType=TIMESTAMP},
            </if>
            <if test="status != null" >
                status = #{status,jdbcType=INTEGER},
            </if>
            <if test="order != null" >
                `order` = #{order,jdbcType=INTEGER}
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <!-- 获取通知列表 -->
    <select id="listNotices" resultType="com.myblog.entity.Notice" parameterType="INTEGER">
        SELECT
        <include refid="Base_Column_List"/>
        FROM <include refid="tb"/>
        <if test="limit != null">
            limit #{limit}
        </if>
    </select>

    <!-- 获取通知总数 -->
    <select id="countNotices" resultType="Integer">
        SELECT COUNT(*) FROM <include refid="tb"/>
    </select>

    <select id="listNoticesByCreateTime" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        <include refid="tb"/>
        ORDER BY
        createTime DESC
    </select>


</mapper>
